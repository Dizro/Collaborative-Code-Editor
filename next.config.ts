import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: [
      'lh3.googleusercontent.com',  // Для Google аватаров
      'api.dicebear.com',          // Для DiceBear аватаров
    ],
  },
}

export default nextConfig;
